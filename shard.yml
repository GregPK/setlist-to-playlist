name: setlist-to-playlist
version: 0.1.0

authors:
  - GregPK <grzegorz.kaczorek@gmail.com>

description: |
  Make a setlist into a playlist.

# development_dependencies:
#   webmock:
#     github: manastech/webmock.cr

targets:
  setlist-to-playlist:
    main: src/setlist-to-playlist.cr


license: MIT

dependencies:
  clim:
    github: at-grandpa/clim
    version: 0.7.0
  pathname:
    github: arcage/crystal-pathname